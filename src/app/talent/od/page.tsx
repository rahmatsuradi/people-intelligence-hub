import React from "react";
import PlaceholderPage from "@/components/placeholder-page";

export default function ODPage() {
  return (
    <PlaceholderPage 
      id="od" 
      title="Organization Development (OD)" 
      description="Visualisasi struktur organisasi (Chart) yang dinamis, pemetaan rentang kendali (span of control), dan evaluasi jabatan (job grading)." 
    />
  );
}
